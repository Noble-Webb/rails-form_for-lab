<% @students.each do |student| %>
  <div><%= student.first_name %></div>
  <div><%= student.last_name %></div>
<% end %>

<% @students.each do |student| %>
  <h1> Looking for Someone?: <%= link_to  "#{student.first_name}, #{student.last_name}",  student_path(student)  %></h1>
<% end %>  